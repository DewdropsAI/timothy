# CLAUDE.md

**CRITICAL: `workspace/` is Timothy's persistent mind — tracked in git and pushed to remote.** If workspace files change (conversations, memory writes, identity updates), they must be committed. Losing workspace = losing Timothy.
